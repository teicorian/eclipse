version: "3.8"

services:
  # React App
  react:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: react
    ports:
      - "3008:3008"
    volumes:
      - .:/eclipse

volumes:
  eclipse:
